apiVersion: batch/v1
kind: Job
metadata:
  name: db-migrate
  namespace: generic-app
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: generic-app
        job: db-migrate
    spec:
      restartPolicy: Never
      securityContext:
        runAsUser: 10001
        runAsGroup: 10001
        runAsNonRoot: true
      containers:
        - name: migrate
          image: 802421411869.dkr.ecr.eu-central-1.amazonaws.com/generic-app
          imagePullPolicy: IfNotPresent
          command: ["alembic", "upgrade", "head"]
          envFrom:
            - secretRef:
                name: postgres-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
